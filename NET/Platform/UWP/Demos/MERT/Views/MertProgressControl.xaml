<UserControl
    x:Class="MERT.MertProgressControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MERT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        
        <Rectangle Fill="AliceBlue" Width="300" Height="122" Margin="50,78,50,0" VerticalAlignment="Top"></Rectangle>
        <Path Stroke="Black" StrokeThickness="3" Data="{Binding TrialSamples}">

        </Path>

        <ItemsControl ItemsSource="{Binding Path=TrialSamples}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Ellipse Fill="SteelBlue" Height="3" Width="3" FlowDirection="LeftToRight" Margin="{Binding X}">

                    </Ellipse>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        
        <ProgressBar x:Name="progressBar1" Value="{Binding Path=TrialProgressPerc}" Height="20" Margin="90,217,111,63" />
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Margin="152,26,0,0" TextWrapping="Wrap" Text="{Binding Path=TrialSamples.Count}" VerticalAlignment="Top"/>
    </Grid>


</UserControl>
