<UserControl x:Class="Treehopper.Mvvm.Views.TreehopperSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="using:Treehopper.Mvvm.ViewModel"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.DataContext>
            <vm:TreehopperSelectorViewModel />
        </Grid.DataContext>
        <ComboBox Grid.Column="0" ItemsSource="{Binding Path=Boards}" SelectedItem="{Binding SelectedBoard, Mode=TwoWay}" IsEnabled="{Binding CanChangeBoardSelection}" HorizontalAlignment="Stretch" />
        <Button Grid.Column="1" Command="{Binding ConnectCommand}" Content="{Binding Path=ConnectButtonText}" />
    </Grid>
</UserControl>
