<?xml version="1.0" encoding="UTF-8"?>
<d:LibraryComponent xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:d="clr-namespace:TreehopperApp.Pages.Libraries"
                    xmlns:jpc="clr-namespace:JPC.BindablePicker"
             x:Class="TreehopperApp.Pages.Libraries.Display">
    <d:LibraryComponent.Configuration>
        <StackLayout>
            <jpc:BindablePicker ItemsSource="{Binding DisplayType}" SelectedItem="{Binding SelectedDisplay}" Title="Displays" />
        </StackLayout>
    </d:LibraryComponent.Configuration>
    <StackLayout>
        <Label Text="Text to display:" />
        <Entry Text="{Binding Text}" />
    </StackLayout>
</d:LibraryComponent>