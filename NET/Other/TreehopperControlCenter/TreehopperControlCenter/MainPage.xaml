<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TreehopperControlCenter"
             xmlns:jpc="clr-namespace:JPC.BindablePicker"
             x:Class="TreehopperControlCenter.MainPage" >
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness"
                    iOS="20, 40, 20, 20"
                    Android="20, 20, 20, 20"
                    WinPhone="20, 20, 20, 20" />
    </ContentPage.Padding>
    <StackLayout Orientation="Vertical">
        <StackLayout Orientation="Horizontal">
            <Label Text="LED" WidthRequest="100"/>
            <Switch x:Name="ledSwitch" />
        </StackLayout>
        <ListView  x:Name="pins">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Vertical">
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Name}" WidthRequest="100" />
                                <jpc:BindablePicker ItemsSource="{Binding PinModes}" SelectedItem="{Binding SelectedPinMode, Mode=TwoWay}" WidthRequest="150"/>
                                <Switch IsToggled="{Binding SwitchValue}" IsEnabled="{Binding SwitchEnabled}" IsVisible="{Binding SwitchVisible}" HorizontalOptions="CenterAndExpand"/>
                                <Slider Value="{Binding SliderValue}" Minimum="0" Maximum="1" IsVisible="{Binding SliderVisible}" HorizontalOptions="FillAndExpand" />
                                <ProgressBar Progress="{Binding ProgressValue}" IsVisible="{Binding ProgressVisible}" HorizontalOptions="FillAndExpand"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>