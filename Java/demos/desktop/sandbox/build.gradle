apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':treehopper.desktop')
    compile project(path: ':treehopper.libraries')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
